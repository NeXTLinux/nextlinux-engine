# source this file if you would like to run functional tests against a local running instance of engine
# that is not within the standard functional compose stack used in CI (make setup-and-test-functional)

# note: you may need to change these values to meet your setup

export NEXTLINUX_TEST_DOCKER_REGISTRY_USER=nextlinux
export NEXTLINUX_TEST_DOCKER_REGISTRY_PASS=foobar
export NEXTLINUX_TEST_DOCKER_REGISTRY_HOST=localhost:5000
export NEXTLINUX_CATALOG_URL=http://localhost:8330/v1
export NEXTLINUX_POLICY_ENGINE_URL=http://localhost:8331/v1
export NEXTLINUX_BASE_URL=http://localhost:8228/v1
export NEXTLINUX_CLI_URL=http://localhost:8228/v1
export NEXTLINUX_CLI_USER=admin
export NEXTLINUX_CLI_PASS=foobar
export NEXTLINUX_TEST_DB_URL=postgresql://postgres:mysecretpassword@localhost:5432/postgres
export TOX_ENV=py38

#export TEST_VULNERABILITIES_PROVIDER=legacy # Uncomment this if wanting to test legacy provider
